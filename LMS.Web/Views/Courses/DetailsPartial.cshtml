@using Microsoft.AspNetCore.Identity
@model LMS.Core.ViewModels.CourseDetailsViewModel
@inject UserManager<ApplicationUser> UserManager


<a asp-action="CoursesListMaterial" asp-controller="Documents" asp-route-id="@Model.Id" class="documentLink m-auto btn btn-primary text-light border border-dark float-right">Document</a>

@*<a class="documentLink m-auto btn btn-primary text-light border border-dark float-right" asp-action="CourseMainPageDocument" asp-controller="Documents" asp-route-id="@Model.Id">Document</a>*@

@if (User.IsInRole("Teacher"))
{
<a asp-action="Create" asp-controller="Documents" asp-route-param="@Model.Id" asp-route-param2="Course" class="float-right m-auto fas fa-folder-plus">Add Document</a>
}
<br />
<table class="table">


    <thead>
        <tr>

            <th>@Html.DisplayNameFor(model => model.Description)</th>
            <th>@Html.DisplayNameFor(model => model.StartDate)</th>
            <th>@Html.DisplayNameFor(model => model.EndDate)</th>
        </tr>
    </thead>
    <tbody>

        <tr>

            <td>@Html.DisplayFor(model => model.Description)</td>
            <td>@Html.DisplayFor(model => model.StartDate)</td>
            <td>@Html.DisplayFor(model => model.EndDate)</td>

            <td>@Html.DisplayFor(model => model.Modules)</td>
        </tr>


    </tbody>

</table>

